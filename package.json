{
  "name": "test-mindsphere-application",
  "version": "1.0.0",
  "description": "Test Mindsphere with self hosted application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "./node_modules/.bin/netlify-lambda build src"
  },
  "author": "Bill",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.15"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
